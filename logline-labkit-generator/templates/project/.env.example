# No provider is required for local CLI use.
LOGLINE_LAB_PROFILE=local-offline

# Supabase profile. Values come from Supabase project settings or the Supabase
# CLI; never commit real keys.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
