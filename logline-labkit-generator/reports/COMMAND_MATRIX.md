# Command Matrix

Generator-side note: the generated project `reports/COMMAND_MATRIX.md` is now rendered from `logline-labkit-generator/blueprints/cli.commands.yaml` after the base project template is copied. Keep this report as a generator-side planning note; update the blueprint for generated command status/scope/authority rows.
