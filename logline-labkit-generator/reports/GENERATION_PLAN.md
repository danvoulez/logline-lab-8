# Generation Plan

1. Preserve the approved corpus in `corpus/`.
2. Compile the corpus rules into blueprints and templates.
3. Generate `dist/logline-lab-kit/` as a CLI-first Rust workspace.
4. Produce reports for ghosts, command behavior, forbidden marker scan, and salvage.
5. Run validation, command matrix, scanner, and Rust checks before final delivery.
